<!DOCTYPE html>
<html>
<head>
  <title>{{title}}</title>
</head>
<body>
  <h1>{{title}}</h1>
  <p>{{description}}</p>

  <ul>
    {{#each trips}}
      <li>
        <strong>{{title}}</strong> — {{location}} — ${{price}}
      </li>
    {{/each}}
  </ul>
</body>
</html>
